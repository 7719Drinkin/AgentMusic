# AgentMusic - Spotify Web UI 对比与参考

更新日期：2026-04-08  
参考目标：[Spotify Web Player](https://open.spotify.com/)

## 1. 目标

本文档用于对比当前 AgentMusic 前端与 Spotify Web 播放器的 UI 细节，明确：

- 当前底部播放器还缺哪些高优先级交互
- 当前代码中是否已有“当前播放栏”结构
- 哪些设计可以直接借鉴
- 哪些功能当前只做 UI 壳，不接业务逻辑

## 2. 当前代码结构结论

### 2.1 已有基础

当前前端已经具备：

- 左侧导航栏
- 中央主内容区
- 固定底部播放器
- 左侧歌单区域
- 底部播放器的基础播放控制和音量条

### 2.2 当前明确缺失

对照 Spotify Web，当前前端还缺少以下播放器细节：

1. “加入歌单/收藏”操作的明确提示
2. 歌词入口
3. 队列入口
4. 打开迷你播放器入口
5. 按钮 hover tooltip
6. 可点击的歌曲名称与歌手名称
7. 点击封面打开右侧“当前播放栏”
8. 独立的“当前播放栏”组件

### 2.3 当前不纳入本轮范围

以下内容明确后置：

1. 连接到设备
2. 设备切换
3. 全屏播放器

原因：

- 这些功能依赖真实 Spotify 设备能力
- 当前主目标是补齐 UI 展示层，不影响链路逻辑

## 3. 当前播放栏代码检查结论

已检查：

- `agentmusic-frontend/src/App.jsx`
- `agentmusic-frontend/src/pages/home.jsx`
- `agentmusic-frontend/src/pages/chat.jsx`
- `agentmusic-frontend/src/component/**`

结论：

- 当前代码中没有独立成型的右侧“当前播放栏”组件
- 也没有“点击封面展开当前播放栏”的状态控制逻辑

因此如果要补齐这部分 UI，需要新增：

1. 一个独立的 `CurrentPlayingPanel` 组件
2. 一个全局开关状态
3. 底部播放器与当前播放栏的联动入口

## 4. 可借鉴的 Spotify Web 设计点

### 4.1 可以直接借鉴

1. 底部图标按钮 hover 时显示 tooltip
2. 曲名和歌手名具备明显可点击语义
3. 封面图作为右侧面板开关入口
4. 底部右侧次级功能入口的统一图标风格
5. 右侧当前播放栏的“标题 + 大图 + 元信息 + 次级区块”布局

### 4.2 可以借鉴，但先只做 UI 壳

1. 歌词入口
2. 队列入口
3. 迷你播放器入口
4. 加入歌单/收藏入口

当前策略：

- 本轮先补按钮与面板骨架
- 不接真实业务逻辑

## 5. 本轮要补齐的点

### Priority 1

1. 给底部播放器按钮增加 tooltip
2. 让歌曲名称和歌手名称可点击
3. 新增右侧当前播放栏骨架
4. 点击专辑封面时切换当前播放栏

### Priority 2

5. 为底部播放器补歌词入口
6. 为底部播放器补队列入口
7. 为底部播放器补迷你播放器入口
8. 为“加入歌单/收藏”入口补更清晰的视觉语义

## 6. Figma 状态

当前 Figma 账号已连接，但权限为：

- `View`

这意味着：

- 可以确认账号连接状态
- 不能直接创建或编辑新的设计文件

因此本轮不能直接让 Figma 复现参考设计。若后续需要在 Figma 中还原这些 UI，需要至少具备可编辑权限。

## 7. 本轮实施原则

1. 只改前端 UI 层
2. 不改变现有前后端链路逻辑
3. 纯展示功能可以先本地状态驱动
4. 歌词、队列、迷你播放器只做前端壳
5. 真实业务接入后再替换占位行为

## 8. 本轮已完成

1. 底部播放器控制按钮已补齐 tooltip
2. 曲名与歌手名已改为可点击入口
3. 专辑封面已可切换右侧当前播放栏
4. 已新增右侧 `CurrentPlayingPanel` 前端骨架
5. 已补歌词 / 队列 / 迷你播放器三个 UI 入口
6. 本轮未接入新的业务逻辑，只补前端展示与交互
