# AgentMusic - Spotify Web UI 对比与参考
更新日期：2026-04-08  
参考目标：[Spotify Web Player](https://open.spotify.com/)

## 1. 目标

本文档用于约束 AgentMusic 当前前端与 Spotify Web UI 的对比与借鉴范围，重点覆盖：

- 底部播放器细节
- 右侧“当前播放栏”
- 独立队列抽屉
- 后续仍需补齐的 UI 细节

原则：

- 只细化前端 UI，不改变当前前后端链路逻辑
- 采用解耦实现，避免把右侧栏、底部播放器和主页面写成一团
- 低优先级功能只做入口或占位，不提前接业务

## 2. 当前 UI 结构

当前前端已经具备：

- 左侧导航与歌单列表
- 中央主内容区
- 底部播放器
- 右侧“当前播放栏”

其中右侧区域现已拆分为三个独立部分：

1. `CurrentPlayingPanel`
   当前播放主面板
2. `CurrentPlayingMenu`
   歌曲更多操作菜单
3. `CurrentPlayingQueueDrawer`
   独立的右侧队列抽屉

## 3. 已经补齐的 Spotify 风格细节

### 3.1 底部播放器

已补齐：

- 按钮 tooltip
- 曲名可点击
- 歌手名可点击
- 点击封面打开右侧当前播放栏
- 队列按钮单独控制右侧队列抽屉
- 迷你播放器保留入口，但降为 Priority 2 占位
- 歌词入口保留，但不在当前播放栏中承担内容展示

### 3.2 当前播放栏

当前播放栏已经按 Spotify Web 结构重组为：

1. 顶部歌单名称栏
   - hover 时显示左侧“隐藏当前播放视图”
   - 右侧显示“更多有关该歌曲的选项”
2. 大封面图
3. 歌曲名与艺人名
4. 艺人信息卡
5. 歌曲提供者卡
6. “队列中的下一首歌”卡片
   - 只展示下一首
   - 左上角提供“打开队列”

### 3.3 队列抽屉

队列不再混入当前播放栏主内容中，而是：

- 在点击底部播放器“队列”按钮时打开
- 在点击右侧当前播放栏“打开队列”时打开
- 以独立抽屉形式从当前播放栏底部向上弹出
- 宽度与当前播放栏一致

## 4. 当前不纳入本轮范围

以下内容明确不在本轮实现：

1. 连接到设备
2. 设备切换
3. 全屏播放器
4. 迷你播放器真实浮窗能力
5. 歌词在右侧栏中的展示

原因：

- 这些能力要么依赖真实 Spotify 设备与 Premium
- 要么依赖主页面新布局设计
- 要么需要桌面级窗口能力，不属于当前 Web UI 细化范围

## 5. 当前播放栏的设计约束

为避免后续继续跑偏，当前播放栏必须遵守以下约束：

1. 不显示播放/暂停状态  
   该职责只属于底部播放器

2. 不承担歌词内容展示  
   歌词应作为主页面功能处理

3. 不直接展示完整队列  
   当前播放栏只展示“队列中的下一首歌”

4. 完整队列必须通过独立抽屉展示

5. 右侧栏必须与底部播放器解耦  
   - 底部播放器负责控制  
   - 右侧栏负责展示与辅助导航

6. 顶部标题区必须浮在右侧栏上方，不跟随内容区滚动  
   - 包括歌单名称  
   - “隐藏当前播放视图”图标  
   - “更多有关当前歌曲的选项”图标  
   这些元素若跟随内容区一起滚动，会导致滚动后无法快速操作，偏离参考 UI。

7. 右侧栏底部必须以上方播放器栏为下边界  
   - 右侧栏不能与整个页面底部平齐  
   - 其可视区域和滚动区域都必须避开底部播放器  
   否则会出现内容被播放器遮挡的问题。

## 9. 2026-04-11：右侧栏布局约束落地

本轮对右侧“当前播放栏”补了两项关键布局修正：

1. 顶部标题区固定在右侧栏上方，不参与内容滚动
   - 歌单名称
   - “隐藏当前播放视图”图标
   - “更多有关当前歌曲的选项”图标

2. 内容区改为独立滚动容器
   - 封面、歌曲信息、艺人卡、提供者卡、下一首卡片在内容区内滚动
   - 滚动条只属于内容区，不属于整个右侧栏外框

3. 右侧栏整体高度重新按底部播放器上边界计算
   - 避免当前播放栏底部被底部播放器遮挡

## 6. 当前更多操作菜单

当前歌曲更多操作菜单保留这些选项：

- 加入歌单
- 加入播放队列
- 推荐此歌曲类型歌单
- 推荐此歌手歌单
- 转至艺人
- 转至专辑
- 查看制作人

当前阶段这些选项主要完成 UI 与信息架构，不全部接业务逻辑。

## 7. 后续待补的 UI 细节

### Priority 1

1. 讨论并确定“聊天推荐后的回显 UI”
2. 侧栏歌单选中态 / 当前播放态增强
3. 当前播放栏中的艺人卡、制作人卡接入真实数据来源

### Priority 2

4. 迷你播放器真实能力
5. 歌词主页面展示
6. 队列抽屉与真实播放上下文联动
7. 设备列表与设备切换

## 8. 实施原则

1. 优先复用现有结构，不重写主链路
2. 新 UI 入口必须通过局部状态管理，不污染全局逻辑
3. 右侧栏变更不得影响左侧侧栏和底部播放器基础样式
4. 视觉对齐 Spotify Web，但不盲目复制其全部功能
