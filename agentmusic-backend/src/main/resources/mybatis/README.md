# MyBatis Mapper XML Location

This directory is reserved for future MyBatis mapper XML files.

The current backend still runs on in-memory repositories, but MyBatis is now the
planned persistence path for MySQL-backed repositories.
