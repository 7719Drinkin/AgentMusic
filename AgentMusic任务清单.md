# AgentMusic 任务清单

版本：V1.0  
日期：2026-04-08

本文档基于以下文档统一整理当前任务状态：

- `AgentMusic需求分析说明书.md`
- `AgentMusic开发笔记.md`
- `AgentMusic-proposal.md`
- `AgentMusic无Premium开发链路说明.md`

## 1. 已完成任务

### Priority 1

#### A. 工程与文档基线

- 已完成后端工程分层整理
- 已完成 Maven 本地仓库与构建目录隔离
- 已完成 API 文档基线
- 已完成需求说明书（SRS）与需求追踪表

#### B. 后端核心结构

- 已完成领域模型、DTO、Repository 抽象与内存实现
- 已完成 Service / Application Service / Controller 分层
- 已完成播放会话模型扩展：
  - `currentPlaylistId`
  - `currentTrackIndex`

#### C. Agent 与推荐基础链路

- 已完成 Agent 聊天接口联通
- 已完成 planner 骨架与推荐意图基本分流
- 已完成推荐歌单创建与版本化保存

#### D. 音乐元数据能力

- 已完成曲目查询
- 已完成歌手查询
- 已完成歌曲搜索

#### E. 前端基础页面

- 已完成聊天主页
- 已完成音乐主页保留与迁移
- 已完成左侧推荐歌单区基础接线
- 已完成底部播放器基础接线

#### F. 无 Premium 本地闭环基础

- 已完成本地 `PlaybackSession` 作为当前播放器权威状态
- 已完成推荐播放路径默认写入本地 session
- 已完成本地上一首 / 下一首 fallback
- 已完成播放器真实歌手名显示
- 已完成播放器状态边界补稳

### Priority 2

- 已完成语音输入 UI 入口占位
- 已完成 live LLM 可开关的实验性接线

## 2. 待完成任务

### Priority 1

#### A. 聊天推荐闭环联调

- 验证聊天推荐后左侧歌单是否自动刷新
- 验证聊天推荐后播放器是否自动切到推荐首曲
- 修正当前普通聊天仍主要返回 planner skeleton/fallback 的问题边界

#### B. 推荐链路质量

- 强化推荐歌单生成逻辑
- 更明确利用：
  - 用户偏好
  - 历史歌单
  - 聊天上下文

#### C. 底部播放器闭环验证

- 验证播放 / 暂停 / 上一首 / 下一首 / seek / 模式切换在本地闭环下的稳定性
- 验证播放器和后端 session 的双向回显是否一致

#### D. 播放器回显设计

- 讨论并确定“推荐完成后当前播放回显”的 UI 表达方式
- 再实现聊天区或播放器区的推荐回显优化

#### E. Spotify bridge 实测闭环

- 完成有效 `client_id / client_secret` 配置
- 完成 bridge 授权
- 确认可用设备
- 再验证真实 Spotify 控制链路

### Priority 2

- 设备列表展示
- 设备切换
- 歌词显示
- 播放队列管理
- 歌手详情页
- 语音识别完整链路
- 流式聊天输出
- MySQL / Redis 持久化替换内存实现

### Priority 3

- 用户个人 Spotify 账号绑定产品化
- 社区功能
- 商业化账号体系
- 多端同步能力

## 3. 当前验证结论

截至 2026-04-08：

1. 聊天区已能收到后端回复，但普通问候语当前仍返回：
   - `Planner skeleton ready. Intent=CHAT_ONLY...`
2. 底部播放器在单曲测试歌单下，上一首 / 下一首已正确禁用
3. 由于聊天推荐链路仍未完成有效回写，暂时不能确认：
   - 左侧歌单是否随推荐自动刷新
   - 底部播放器是否会自动切到推荐首曲

## 4. 接下来应该完成的任务

### 第一优先级

1. 打通聊天推荐的真实推荐闭环
   - 目标：不再只验证聊天接口通，而是验证“推荐 -> 歌单刷新 -> 播放器切曲”

2. 联调并修正聊天推荐后的前端自动回写
   - 左侧歌单刷新
   - 底部播放器切曲
   - 会话同步事件触发

3. 讨论并确定推荐完成后的回显 UI 方案
   - 先定设计，再改代码

### 第二优先级

4. 在当前已稳定的本地播放器基础上，继续验证本地控制闭环
   - 播放 / 暂停
   - 上一首 / 下一首
   - seek
   - 模式切换

5. 等 bridge 配置准备好后，再做真实 Spotify 播放控制验证

### 当前不建议立刻推进的任务

- 设备列表与设备切换
- 流式输出
- 语音识别完整链路
- MySQL / Redis 完整持久化替换

这些任务不直接影响当前主闭环，应继续后置。
## 2026-04-12 迭代补充：任务优先级重排与第一轮完成情况

### 已完成

- 第一轮“真实数据回填”已完成：
  - 右侧当前播放栏改为基于真实播放上下文渲染
  - 当前播放栏顶部显示真实歌单名称
  - 当前曲目、艺人名称、队列中的下一首歌均来自真实推荐歌单数据
  - 歌曲提供者区域不再为空，已至少回填 Main Artist，并对 Spotify 未提供的字段作显式说明
  - 新增歌单详情接口 `GET /api/playlists/{playlistId}/detail`

### 重新排定后的优先级

#### Priority 1

1. 第一轮：真实数据回填
   - 已完成
2. 第二轮：真实 Spotify 播放控制
   - 目标：解决“歌曲无法播放”的核心问题
   - 包括：播放 / 暂停 / 上一首 / 下一首 / seek / 模式切换
3. 第三轮：歌单页与侧栏交互
   - 目标：点击左侧推荐歌单进入真实歌单页
   - 目标：将左侧推荐歌单 UI 调整为正式样式

#### Priority 2

1. MySQL / Redis 持久化落地
   - playlists
   - playlist_tracks
   - sessions
   - chat_messages
   - tracks
   - artists
2. 设备列表 UI
3. 设备切换 UI

#### Priority 3

1. 歌词主页面展示
2. 流式聊天输出
3. 语音识别完整链路
4. 真实 LLM 聊天链路接入
## 2026-04-12 补充：歌曲提供者信息优先级调整

经确认，当前 Spotify Web API 公开接口不提供 `Lyricist`、`Composer`、`Producer credits` 这类完整歌曲署名字段。

因此将“歌曲提供者卡片中的高级 credits 完整显示”调整为较低优先级事项：

- 当前阶段仅保留：
  - `Main Artist`
  - `Album`
- 以下内容降为 `Priority 3`：
  - `Lyricist`
  - `Composer`
  - `Producer`
  - 其他完整制作人员 credits

当前主线不再围绕该区域继续补 controller 或 Spotify 接口，而优先推进真实 Spotify 播放控制闭环。

## 2026-04-12 补充：重大缺陷（Bridge 模式播放上下文共享）

### 缺陷描述

- 当前系统使用单一 Spotify bridge 账号承载真实播放控制。
- 多个用户会共享同一套 Spotify 播放上下文，无法同时各自播放不同歌曲。
- 后一个用户的真实播放控制会覆盖前一个用户的播放状态。

### 当前处理原则

- 当前课程设计阶段接受该限制，用于验证“单共享播放实例”的主链路。
- 所有文档、演示和答辩表述都必须明确说明这一点，避免将当前版本误表述为“多用户独立 Spotify 播放”。

### 后续修复方向

- 修复该重大缺陷需要升级为“每个用户绑定各自 Spotify 账号”的授权模型。
- 该修复方向当前归入 `Priority 3`，不阻塞当前播放控制与推荐闭环的实现。

## 2026-04-12 补充：真实播放控制任务推进

### 已完成

- 已完成设备发现与显式 `transfer playback` 接口。
- 已修正播放控制请求 URI，避免误打到 `localhost:80`。
- 已将播放模式切换从 `playTrack` 主路径中解耦为非阻塞步骤。
- 已将 `next / previous` 收敛到本地歌单上下文模型，并补充回归测试。
- 已将前端远程同步间隔从 `4000ms` 收紧到 `1500ms`。

### 当前进行中（Priority 1）

1. 稳定 `pause / play / sync` 的真实 Spotify 行为
2. 验证 `next / previous` 在本地歌单上下文下切换真实曲目
3. 验证 `seek`
4. 验证 `mode`

### 当前暂不推进

- Playwright 页面级自动化验证
  - 当前会话内浏览器自动化入口仍不稳定
  - 本轮以真实 API 验证和后端测试为主
