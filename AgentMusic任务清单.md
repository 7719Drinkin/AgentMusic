# AgentMusic 任务清单

版本：V1.0  
日期：2026-04-08

本文档基于以下文档统一整理当前任务状态：

- `AgentMusic需求分析说明书.md`
- `AgentMusic开发笔记.md`
- `AgentMusic-proposal.md`
- `AgentMusic无Premium开发链路说明.md`

## 1. 已完成任务

### Priority 1

#### A. 工程与文档基线

- 已完成后端工程分层整理
- 已完成 Maven 本地仓库与构建目录隔离
- 已完成 API 文档基线
- 已完成需求说明书（SRS）与需求追踪表

#### B. 后端核心结构

- 已完成领域模型、DTO、Repository 抽象与内存实现
- 已完成 Service / Application Service / Controller 分层
- 已完成播放会话模型扩展：
  - `currentPlaylistId`
  - `currentTrackIndex`

#### C. Agent 与推荐基础链路

- 已完成 Agent 聊天接口联通
- 已完成 planner 骨架与推荐意图基本分流
- 已完成推荐歌单创建与版本化保存

#### D. 音乐元数据能力

- 已完成曲目查询
- 已完成歌手查询
- 已完成歌曲搜索

#### E. 前端基础页面

- 已完成聊天主页
- 已完成音乐主页保留与迁移
- 已完成左侧推荐歌单区基础接线
- 已完成底部播放器基础接线

#### F. 无 Premium 本地闭环基础

- 已完成本地 `PlaybackSession` 作为当前播放器权威状态
- 已完成推荐播放路径默认写入本地 session
- 已完成本地上一首 / 下一首 fallback
- 已完成播放器真实歌手名显示
- 已完成播放器状态边界补稳

### Priority 2

- 已完成语音输入 UI 入口占位
- 已完成 live LLM 可开关的实验性接线

## 2. 待完成任务

### Priority 1

#### A. 聊天推荐闭环联调

- 验证聊天推荐后左侧歌单是否自动刷新
- 验证聊天推荐后播放器是否自动切到推荐首曲
- 修正当前普通聊天仍主要返回 planner skeleton/fallback 的问题边界

#### B. 推荐链路质量

- 强化推荐歌单生成逻辑
- 更明确利用：
  - 用户偏好
  - 历史歌单
  - 聊天上下文

#### C. 底部播放器闭环验证

- 验证播放 / 暂停 / 上一首 / 下一首 / seek / 模式切换在本地闭环下的稳定性
- 验证播放器和后端 session 的双向回显是否一致

#### D. 播放器回显设计

- 讨论并确定“推荐完成后当前播放回显”的 UI 表达方式
- 再实现聊天区或播放器区的推荐回显优化

#### E. Spotify bridge 实测闭环

- 完成有效 `client_id / client_secret` 配置
- 完成 bridge 授权
- 确认可用设备
- 再验证真实 Spotify 控制链路

### Priority 2

- 设备列表展示
- 设备切换
- 歌词显示
- 播放队列管理
- 歌手详情页
- 语音识别完整链路
- 流式聊天输出
- MySQL / Redis 持久化替换内存实现

### Priority 3

- 用户个人 Spotify 账号绑定产品化
- 社区功能
- 商业化账号体系
- 多端同步能力

## 3. 当前验证结论

截至 2026-04-08：

1. 聊天区已能收到后端回复，但普通问候语当前仍返回：
   - `Planner skeleton ready. Intent=CHAT_ONLY...`
2. 底部播放器在单曲测试歌单下，上一首 / 下一首已正确禁用
3. 由于聊天推荐链路仍未完成有效回写，暂时不能确认：
   - 左侧歌单是否随推荐自动刷新
   - 底部播放器是否会自动切到推荐首曲

## 4. 接下来应该完成的任务

### 第一优先级

1. 打通聊天推荐的真实推荐闭环
   - 目标：不再只验证聊天接口通，而是验证“推荐 -> 歌单刷新 -> 播放器切曲”

2. 联调并修正聊天推荐后的前端自动回写
   - 左侧歌单刷新
   - 底部播放器切曲
   - 会话同步事件触发

3. 讨论并确定推荐完成后的回显 UI 方案
   - 先定设计，再改代码

### 第二优先级

4. 在当前已稳定的本地播放器基础上，继续验证本地控制闭环
   - 播放 / 暂停
   - 上一首 / 下一首
   - seek
   - 模式切换

5. 等 bridge 配置准备好后，再做真实 Spotify 播放控制验证

### 当前不建议立刻推进的任务

- 设备列表与设备切换
- 流式输出
- 语音识别完整链路
- MySQL / Redis 完整持久化替换

这些任务不直接影响当前主闭环，应继续后置。
