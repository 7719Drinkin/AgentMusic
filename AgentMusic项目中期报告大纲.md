# AgentMusic 项目中期报告大纲

版本：M1.0  
日期：2026-05-14  
用途：用于中期汇报 PPT 制作的大纲与内容素材

## 1. 项目概述

AgentMusic 是一个基于自然语言 Agent 的音乐推荐与播放控制系统。项目目标不是重新实现一个完整音乐平台，而是验证一条完整的智能音乐交互闭环：

1. 用户通过自然语言表达音乐需求。
2. Agent 理解需求并生成推荐歌单。
3. 系统保存推荐结果与播放会话。
4. 前端展示聊天结果、推荐歌单、当前播放状态和队列。
5. 后端通过 Spotify bridge 账号控制真实 Spotify 设备播放。

当前项目已经从“前后端基础连通”推进到“真实 LLM + 真实 Spotify 播放 + MySQL/Redis 持久化”的中期阶段。

## 2. 开发方法

### 2.1 垂直闭环优先

本项目采用“垂直闭环优先”的迭代方式，而不是先一次性完成所有页面和功能。

早期优先打通最小可用闭环：

- 聊天页输入推荐需求。
- 后端识别推荐意图。
- 生成推荐歌单。
- 写入播放会话。
- 前端展示推荐结果。
- Spotify 设备执行真实播放。

在主闭环跑通后，再逐步补充持久化、错误处理、设备切换、UI 细节、LLM 接入和 E2E 验证。

### 2.2 文档驱动的开发上下文管理

项目开发过程中使用了类 Harness 的方法管理需求、约束和开发上下文。

具体做法是将关键设计和开发状态持续写入文档，并让 Codex 在后续开发中以这些文档作为上下文指导，减少重复解释和上下文漂移。

当前主要上下文文档包括：

- `AgentMusic开发笔记.md`：记录当前开发阶段、已完成能力、架构变化和限制。
- `AgentMusic任务清单.md`：记录优先级、已完成任务、待完成任务和下一步执行顺序。
- `AgentMusic需求分析说明书.md`：记录系统边界、核心功能需求和非功能需求。
- `agentmusic-backend/docs/planner-design-spec.md`：记录 Agent planner 的设计约束。
- `agentmusic-backend/docs/llm/planning-harness.md`：记录 LLM 输出 Harness 的输入输出规则与校验逻辑。
- `agentmusic-backend/docs/recommendation-search-chain.md`：记录推荐链路、Spotify 搜索策略和候选重排职责。

这种方法的作用是：

- 将需求、约束、历史决策显式化。
- 降低长周期开发中的上下文丢失风险。
- 让 Codex 在每轮开发中可以读取项目文档，自我校准当前阶段目标。
- 让开发过程更接近“需求 Harness + 代码 Harness”的双重约束模式。

### 2.3 LLM Harness 约束

项目没有直接信任 LLM 的自由文本输出，而是为 LLM 设计了严格的 Harness。

LLM 输出必须满足：

- 固定 JSON 格式。
- 固定 schemaVersion。
- 意图必须属于白名单。
- steps 必须匹配预定义模板。
- arguments 必须通过字段级校验。
- 只有成功转换为 `AgentPlan` 后才能进入执行链。

如果 LLM 输出不合法，系统会回退到确定性 planner，保证主链路不会因为 LLM 随机输出而崩溃。

### 2.4 混合智能推荐方法

推荐链采用“LLM + 确定性代码”的混合模式。

LLM 负责：

- 理解用户自然语言需求。
- 提炼推荐规格 `RecommendationSpec`。
- 判断请求类型，例如歌手推荐、专辑推荐、主题推荐。
- 对候选歌曲进行语义重排。

代码负责：

- Spotify 候选召回。
- 歌手、歌曲、专辑的硬边界过滤。
- 显式歌曲优先插入。
- 去重与最终可播放校验。
- 播放会话写入与真实设备控制。

这样可以兼顾 LLM 的语义理解能力和代码规则的稳定性。

### 2.5 渐进式架构演进

项目没有一开始就设计成完全体，而是按风险逐步演进：

- 先使用内存仓储快速验证业务闭环。
- 再通过 Repository 抽象切换到 MyBatis + MySQL。
- Redis 用于播放会话热状态和缓存。
- Spotify bridge token 暂时使用文件持久化。
- 前端先迁移 Spotify 风格界面，再逐步调整为 AgentMusic 交互结构。

### 2.6 测试与验证方法

当前验证方式包括：

- 后端单元测试：验证 planner、推荐链、播放服务、持久化仓储。
- 接口联调：直接请求 `/api/agent/chat`、`/api/playback`、`/api/playlists` 等接口。
- 浏览器 E2E：通过 Playwright / Edge CDP 验证页面交互。
- 真实设备联调：使用 Spotify bridge 账号验证播放、暂停、切歌、设备切换。
- 数据库验证：检查 MySQL 中 playlists、tracks、artists、sessions、chat_messages 等表的数据变化。

## 3. 开发历程与解决的问题

### 3.1 第一阶段：项目基础与前端迁移

完成内容：

- 建立 React + Vite 前端项目。
- 建立 Spring Boot 后端项目。
- 引入 Spotify 风格页面作为 UI 基础参考。
- 初步形成聊天页、音乐主页、歌单页、底部播放器等页面结构。

解决的问题：

- 明确系统不是传统音乐播放器，而是 Agent 驱动的音乐交互系统。
- 将 Search、Library 等传统入口逐步弱化，把核心入口收敛到 Agent Chat 和 Music Home。

### 3.2 第二阶段：推荐歌单闭环

完成内容：

- 聊天页接入后端真实接口。
- 中文推荐意图进入推荐分支。
- 后端生成推荐歌单并写入播放会话。
- 左侧栏展示推荐歌单历史。
- 点击歌单进入真实歌单详情页。

解决的问题：

- 解决了前端页面与后端数据割裂的问题。
- 解决了推荐歌单无法持久展示的问题。
- 解决了歌单页只展示静态 demo 数据的问题。

### 3.3 第三阶段：Spotify 真实播放控制

完成内容：

- 接入 Spotify bridge 模式。
- 完成设备发现、播放、暂停、上一首、下一首、seek。
- 支持播放模式切换。
- 支持设备面板和设备状态显示。
- 修复 DNS resolver 与 Spotify API 请求异常。

解决的问题：

- 解决了系统只能模拟播放、不能控制真实 Spotify 设备的问题。
- 解决了播放请求误发到错误地址的问题。
- 解决了设备离线、授权缺失、受限设备等异常状态提示不清的问题。

### 3.4 第四阶段：持久化落地

完成内容：

- 引入 MyBatis、MySQL、Redis。
- 完成 Playlist、Track、Artist、Session、ChatMessage、User 的仓储实现。
- 建立 schema.sql、migration、schema_migrations 和启动校验。
- SessionRepository 采用 Redis + MySQL 混合模式。

解决的问题：

- 解决了刷新或重启后数据丢失的问题。
- 解决了内存仓储无法支持真实演示的问题。
- 解决了数据库结构变更缺乏校验和迁移机制的问题。

### 3.5 第五阶段：真实 LLM 接入

完成内容：

- 接入 OpenAI 兼容接口，并配置 Kimi 模型。
- 设计并实现 LLM planning Harness。
- 将 LLM planner 接入主聊天链路。
- 推荐链职责调整为 LLM 提炼需求、代码召回候选、LLM 重排候选。
- 实现 Kimi 流式输出首版能力。

解决的问题：

- 解决了早期 planner 主要依赖本地规则、智能性不足的问题。
- 解决了 LLM 输出不可控的问题。
- 解决了历史上下文污染和 prompt 示例偏置导致推荐重复的问题。

### 3.6 第六阶段：推荐准确度与播放体验修复

完成内容：

- `artist-only` 请求改为 artistId -> albums -> tracks 的搜索链。
- `artist + track (+ album)` 支持结构化 Spotify query。
- `album-only` 请求加入专辑硬边界。
- 主题型推荐进入独立 `THEME_AWARE` 模式。
- 用户没有说“不要播放”时，推荐后默认开始播放。
- 播放完一首歌后自动跳到下一首。

解决的问题：

- 解决了推荐张雨生歌曲时混入其他歌手的问题。
- 解决了专辑内推荐溢出到专辑外的问题。
- 解决了同名歌曲被错误歌手命中的问题。
- 解决了推荐后不自动播放的问题。
- 解决了歌曲播放完不会自动下一首的问题。

## 4. 当前完成情况

### 4.1 Agent Chat

当前已完成：

- 中文自然语言输入。
- 聊天历史保存。
- LLM planner 主链路。
- Kimi 流式输出首版。
- 推荐请求自动生成歌单。
- 推荐后默认播放。

### 4.2 推荐系统

当前已完成：

- 歌手推荐。
- 歌曲精确推荐。
- 歌手 + 歌曲 + 专辑联合约束。
- 专辑内歌曲推荐。
- 主题型推荐首版。
- LLM 语义归一化。
- LLM 候选重排。
- Spotify 候选召回与硬过滤。

### 4.3 歌单系统

当前已完成：

- 推荐歌单生成。
- 推荐歌单历史展示。
- 左侧歌单滚动。
- 歌单详情页。
- 页内点歌。
- 歌单与播放会话绑定。

### 4.4 播放系统

当前已完成：

- 真实 Spotify 播放。
- 播放 / 暂停。
- 上一首 / 下一首。
- 播放结束自动下一首。
- 进度同步与 seek。
- 播放模式切换。
- 当前播放栏。
- 队列抽屉。

### 4.5 设备系统

当前已完成：

- Spotify 设备发现。
- 设备切换。
- 当前设备显示。
- 离线设备提示。
- 授权异常提示。
- 结构化错误码首版。

### 4.6 Music Home

当前已完成：

- 推荐内容聚合首页。
- 最近推荐歌单展示。
- 推荐歌曲展示。
- 推荐艺人展示。
- 推荐专辑展示。
- 分区展开 / 收起。

### 4.7 数据持久化

当前已完成：

- MySQL 保存歌单、歌曲、艺人、聊天记录、播放会话。
- Redis 保存播放会话热状态。
- MyBatis Repository 替换内存仓储。
- 启动时 schema bootstrap。
- migration 扫描与执行。

## 5. 当前限制

当前系统仍存在以下限制：

- Spotify bridge 模式使用单一 Spotify 账号，不支持多用户独立播放。
- 主题型推荐质量仍需要继续优化。
- 推荐歌单命名还需要改为确定性命名，避免标题过长。
- 自动下一首目前主要依赖前端在线触发，后续可以升级为后端状态同步。
- LLM Agent 还不是完整自主工具调用 Agent，目前仍是受控 planner + executor 模式。
- 部分 UI 细节仍需继续 polish，例如播放器、设备面板、移动端适配和动态效果。

## 6. 未来开发预期

### 6.1 推荐质量提升

下一阶段优先继续提升推荐准确度：

- 强化主题型推荐召回。
- 增加年代、语种、风格、地区等 metadata 约束。
- 优化 Spotify 候选排序。
- 增强 LLM rerank 的可解释性。
- 记录候选被过滤和入选的原因。

### 6.2 Agent 能力增强

后续 Agent 应从“能推荐”升级为“能解释、能调整、能连续对话”：

- 输出“正在理解需求、搜索候选、筛选歌曲、生成歌单、开始播放”等阶段状态。
- 支持用户基于上一张歌单继续修改。
- 支持更自然的 follow-up，例如“换成粤语版”“不要现场版”“多来点冷门的”。
- 在安全可控前提下展示部分思考或决策依据。

### 6.3 播放链路增强

后续播放系统需要继续增强：

- 将自动下一首从前端触发升级为后端播放状态同步。
- 考虑使用 Spotify queue/context 机制减少本地会话与远端播放状态不一致。
- 完善多设备切换场景。
- 增强断线、离线、授权过期后的恢复体验。

### 6.4 工程可观测性

后续需要补充更完整的日志和指标：

- LLM planningSource。
- semantic normalization source。
- candidate recall 数量。
- rerank source。
- guardrail 命中情况。
- Spotify API 错误类型。
- 播放失败原因。

### 6.5 产品功能扩展

长期方向包括：

- 多用户独立 Spotify 授权。
- 歌词功能。
- 更完整的音乐知识问答。
- 更细致的 Music Home 推荐内容沉淀。
- 移动端适配。
- 更接近 Spotify 原版体验的动效和交互。

## 7. PPT 建议结构

建议中期汇报 PPT 可以拆成以下页面：

1. 项目背景与目标。
2. 核心使用场景。
3. 当前系统架构。
4. 开发方法：垂直闭环 + 文档 Harness + LLM Harness。
5. 开发历程一：前后端与推荐闭环。
6. 开发历程二：Spotify 真实播放与设备控制。
7. 开发历程三：持久化与 LLM 接入。
8. 当前完成的主要功能。
9. 当前演示链路。
10. 当前限制与风险。
11. 后续开发计划。
12. 总结。

## 8. 中期结论

AgentMusic 当前已经完成从“静态音乐前端”到“自然语言 Agent 推荐 + 真实 Spotify 播放 + 持久化存储”的核心转变。

项目当前的主要成果不是单个页面或接口，而是一条已经跑通的完整智能音乐闭环：

用户需求 -> LLM 理解 -> 推荐规格 -> Spotify 候选召回 -> LLM 重排 -> 歌单生成 -> 数据持久化 -> 前端展示 -> 真实设备播放。

下一阶段的重点将从“功能跑通”转向“推荐质量、Agent 交互体验、播放稳定性和可观测性”。
